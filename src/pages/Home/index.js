import React from 'react';
import Header from '../Header';
import Filter from '../Filter';
import { Repos } from '../Repos';

const Home = () => {
    return (
        <div>
            <Header />
            <Repos />
        </div>
    )
}

export default Home;
